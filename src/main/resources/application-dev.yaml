# dev environment（生产环境）
server:
  port: 8123
  servlet:
    context-path: /api
    # cookie 7 天过期
    session:
      cookie:
        max-age: 604800
  tomcat:
    max-connections: 1000
    accept-count: 100
    connection-timeout: 20000
spring:
  application:
    name: youyou-erp-system
  # 文件上传设置
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
      enabled: true
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai  # 统一Jackson序列化时区（东八区）
  # 数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/youyou-erp-system?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    # HikariCP 连接池配置
    hikari:
      # 最小空闲连接数
      minimum-idle: 5
      # 最大连接池大小
      maximum-pool-size: 20
      # 连接超时时间（毫秒）
      connection-timeout: 30000
      # 空闲连接存活最大时间（毫秒）
      idle-timeout: 600000
      # 连接最大存活时间（毫秒）
      max-lifetime: 1800000
      # 连接测试查询
      connection-test-query: SELECT 1
  # Session 配置
  session:
    redis:
      namespace: "xiaorui:session" # 定义存储在 Redis 中的 session 数据的命名空间
      flush-mode: on_save  # 每次保存或更新 session 时立即将数据同步到 Redis
      save-mode: always  # 每次请求结束时都保存 session
  # Redis 配置
#  data:
#    redis:
#      host: 127.0.0.1
#      port: 6379
#      database: 8
#      # Redis 密码，如果有的话
#      #password: 123456
#      timeout: 10000ms
#      lettuce:
#        pool:
#          max-active: 50
#          max-idle: 20
#          min-idle: 10
#          max-wait: 10000ms
#        shutdown-timeout: 200ms
mybatis-plus:
  configuration:
    # MyBatis 配置
    map-underscore-to-camel-case: true
    # 仅在开发环境开启日志
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    # 是否开启 banner 打印
    banner: false
# springdoc-openapi（4 版本的 knife4j 接口文档依赖于 springdoc）
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.xiaorui.youyouerpsystem.controller
# knife4j 接口文档
knife4j:
  enable: true
  setting:
    language: zh_cn


